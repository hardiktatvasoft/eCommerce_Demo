    <% if @cms_page.bannerimage.present? %>
    <header class="masthead mb-5" style="background-image: url(<%= @cms_page.bannerimage %>);">
    </header>
    <% end %>
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h1 class="text-center"><%= @cms_page.title %></h1>
                <br>
                <div>
                    <%= sanitize @cms_page.content.html_safe %>
                </div>
            </div>
        </div>
    </div>
    